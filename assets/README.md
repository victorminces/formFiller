# Assets

Put local-only assets here, such as `provider_signature.png`.

`provider_signature.png` is intentionally ignored by git because it may contain a real provider signature. The form filler will use it when present, but it can also run without it or use a signature path provided in the input JSON.
